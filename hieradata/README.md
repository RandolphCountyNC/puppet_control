# Hiera Encrypted YAML
Instructions for creating an encrypted string for EYAML

SSH: rcforeman01.co.randolph.nc.us

cd /etc/puppetlabs/puppet/ssl

**String:**   eyaml encrypt -s *string*

**Password:** eyaml encrypt -p
